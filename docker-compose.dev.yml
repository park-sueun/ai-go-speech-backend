services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.dev
    ports:
      - "8080:8080"
      - "5005:5005"      # 원격 디버그 포트
    environment:
      JAVA_OPTS: -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=*:5005
    env_file:
      - .env.dev
    volumes:
      - ./logs:/app/logs
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started

  db:
    # 호스트 포트 바인딩 없이 내부 전용으로 실행 (로컬 3306 충돌 방지)
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
      MYSQL_DATABASE: ${DB_NAME}
    env_file:
      - .env.dev
    volumes:
      - dev_db_data:/var/lib/mysql

  redis:
    image: redis
    networks:
      - app-network

volumes:
  dev_db_data:
    driver: local
